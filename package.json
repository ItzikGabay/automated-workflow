{
  "name": "root",
  "private": true,
  "version": "0.0.3",
  "scripts": {
    "start": "lerna run start --parallel",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run start --parallel",
    "dev:next": "cd ./apps/app-client && npm run dev"

  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "2.5.1"
  },
  "workspaces": [
    "apps/*",
    "lib/*"
  ]
}
